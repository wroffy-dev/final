node_modules
.next
.git
.github
out
dist
coverage
*.log
.env
.env.local
.env.*.local
public/uploads
backups

# Documentation and tests are not needed to build or run the image. Excluding
# them keeps the build context small, which is what makes the layer cache
# effective — a docs edit should not invalidate `npm ci`.
docs
tests
vitest.config.*
README.md
ARCHITECTURE.md

# Generated by the build inside the image; a stale local copy must not be
# copied in and silently used instead.
prisma/seed.mjs
